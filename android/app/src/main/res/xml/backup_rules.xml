<?xml version="1.0" encoding="utf-8"?>
<!-- Backup is disabled in the manifest; the device secret must not leave the device. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="datastore" />
</full-backup-content>
